// Placeholder. Le widget test par défaut généré par `flutter create` testait
// une counter app qui n'existe pas dans Voyage. À remplacer par de vrais
// tests d'intégration quand on en aura besoin (notamment pour CI).
//
// Pour l'instant : test trivial qui passe — assure juste que la suite
// `flutter test` ne casse pas et que le panel Problems VSCode reste clean.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, equals(2));
  });
}
